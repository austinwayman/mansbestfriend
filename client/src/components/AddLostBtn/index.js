import React from "react";


export function LostAddBtn(props) {
    return (
      
      <button className = "btn btn-primary" onClick = {props.toggle}>Add Thread</button>
  
            );
        }

export default LostAddBtn;
        